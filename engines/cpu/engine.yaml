name: cpu
summary: CPU-optimized for workstations
vendor: Canonical Ltd
devices:
  anyof:
    - type: cpu
      architecture: amd64
    - type: cpu
      architecture: arm64

runtime: llamacpp

model:
  default: glm-ocr-q8-0
  options:
    - glm-ocr-q8-0

configurations:
  sleep-idle-seconds: 600
  min-context-size: 4096
